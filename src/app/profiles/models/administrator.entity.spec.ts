import { AdministratorEntity } from './administrator.entity';

describe('AdministratorEntity', () => {
  it('should create an instance', () => {
    expect(new AdministratorEntity()).toBeTruthy();
  });
});
